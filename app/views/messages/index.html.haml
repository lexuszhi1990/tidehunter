#messages.col-xs-6.col-sm-4.col-md-6.col-md-offset-3
  %table.table.table-hover.col-sm-4
    %tr
      %td="The time list of SR501"
    %tbody
      - collection.all.each do |msg|
        %tr
          %td= "some has come at #{msg.created_at.strftime('%A %b %-d, %Y %H:%M:%S')}"
